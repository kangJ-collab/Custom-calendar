# 데이터 마이그레이션 가이드

제품 버전은 `1.0.0`이고 내부 상태는 기존 Stage 저장 키를 순서대로 탐색해 현재 저장 키로 이전합니다.

## 유지되는 데이터

- 날짜 상태
- 기록 항목
- 반복 패턴
- 날짜별 기록과 메모
- 계산 결과
- 사용자 템플릿
- 테마와 화면 구성

## 정리되는 데이터

- 존재하지 않는 상태·기록 항목을 참조하는 값
- 잘못된 날짜 키
- 허용 범위를 벗어난 배열 항목
- 중복 또는 유효하지 않은 ID
- 지원하지 않는 색상·입력 형식

## 마이그레이션 전

기존 앱의 데이터 내보내기로 JSON 백업을 먼저 보관하는 것을 권장합니다.

## 롤백

새 버전에서 만든 데이터는 이전 버전이 이해하지 못하는 속성을 포함할 수 있습니다. 이전 버전으로 돌아갈 때는 이전 버전에서 만든 백업을 사용하는 것이 안전합니다.
