# 기여 안내

1. 이슈에서 변경 목적과 재현 절차를 먼저 설명합니다.
2. 사용자 화면에는 내부 ID, schema, operand 같은 개발자 용어를 노출하지 않습니다.
3. 사용자 문자열을 `innerHTML`로 삽입하지 않습니다.
4. `eval`, `new Function`, 원격 코드 실행을 사용하지 않습니다.
5. 날짜 계산은 로컬 날짜 생성과 UTC 일수 차이를 구분합니다.
6. 변경 후 `node --check js/app.js`, `node --check js/pwa.js`, 브라우저 스모크 테스트를 수행합니다.
7. 실제 기기에서 시험하지 않은 결과는 PASS로 기록하지 않습니다.
