# 배포 가이드

## GitHub Pages

1. 새 저장소를 만듭니다.
2. ZIP 안의 파일과 폴더를 저장소 루트에 업로드합니다.
3. 저장소 `Settings → Pages`에서 `Deploy from a branch`를 선택합니다.
4. `main` 브랜치와 `/ (root)`를 선택합니다.
5. 표시된 HTTPS 주소에서 앱을 엽니다.

상대 경로를 사용하므로 `https://사용자.github.io/저장소이름/` 같은 하위 경로에서도 동작합니다.

업데이트 후 이전 파일이 보이면 브라우저에서 업데이트 확인을 누르거나 사이트 데이터를 지운 뒤 다시 엽니다.

## Cloudflare Pages

1. Pages 프로젝트를 만듭니다.
2. Git 저장소를 연결하거나 정적 파일 업로드를 선택합니다.
3. 빌드 명령은 비워둡니다.
4. 출력 디렉터리는 저장소 루트를 사용합니다.
5. 배포 후 manifest, 서비스 워커, 오프라인 새로고침을 확인합니다.

## 주의

서비스 워커는 HTTPS 또는 localhost에서만 동작합니다. 단순 파일 열기로는 설치·오프라인 기능을 시험할 수 없습니다.
